name: Update Latest News (scheduled scrape)
on:
  schedule:
    # Run daily at 08:00 Malaysia time (UTC+8) = 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger
permissions:
  contents: write  # Required for git push (otherwise blog never updates from scheduled run)
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Fetch news from RSS sources and apply customizations
        run: npm run update-news

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update site from WordPress (scheduled scrape)"
            git push
          fi
